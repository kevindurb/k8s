---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: consignment-app
  namespace: argocd
spec:
  project: apps
  sources:
    - repoURL: https://github.com/kevindurb/k8s.git
      targetRevision: HEAD
      path: apps/consignment-app
  destination:
    server: https://kubernetes.default.svc
    namespace: consignment-app
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      enabled: true
      prune: false
      selfHeal: true
