---
apiVersion: v1
kind: ConfigMap
metadata:
  name: external-dns-coredns-etcd-config
  namespace: dns-system
data:
  coredns-etcd.json: |
    {
      "endpoints": ["http://coredns-local-etcd.dns-system.svc.cluster.local:2379"],
      "prefix": "/skydns"
    }