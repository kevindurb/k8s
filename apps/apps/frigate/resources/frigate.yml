---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment
  annotations:
    reloader.stakater.com/auto: 'true'
  labels: &labels
    app.kubernetes.io/component: web
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:
      runtimeClassName: nvidia
      nodeSelector:
        kubernetes.io/hostname: drone-04
        nvidia.com/gpu.present: 'true'
      volumes:
        - name: volume
          persistentVolumeClaim:
            claimName: volume
        - name: media
          persistentVolumeClaim:
            claimName: media
        - name: dev-shm
          emptyDir:
            medium: Memory
            sizeLimit: 1G
        - name: config
          configMap:
            name: config
      initContainers:
        - name: download-model
          image: busybox:1.36
          command: ['/bin/sh', '-c']
          args:
            - |
              MODEL_PATH="/config/yolo_nas_s_int8.onnx"
              MODEL_URL="https://github.com/Deci-AI/super-gradients/releases/download/v3.1.1/yolo_nas_s_int8.onnx"

              if [ -f "$MODEL_PATH" ]; then
                echo "Model found at $MODEL_PATH. Skipping download."
              else
                echo "Model not found. Downloading from $MODEL_URL..."
                wget -O "$MODEL_PATH" "$MODEL_URL"
                if [ $? -eq 0 ]; then
                  echo "Download complete."
                  chmod 666 "$MODEL_PATH"
                else
                  echo "Download failed!"
                  exit 1
                fi
              fi
          volumeMounts:
            - name: config
              mountPath: /config
      containers:
        - name: frigate
          image: ghcr.io/blakeblackshear/frigate:0.16.3-tensorrt@sha256:5f3150afac88b13b6a39e19ab18c68f7b84a0d3b8675e58aace0c55c69d98909
          resources:
            requests:
              memory: 100M
            limits:
              memory: 4G
          volumeMounts:
            - name: volume
              mountPath: /config
            - name: media
              mountPath: /media
            - name: dev-shm
              mountPath: /dev/shm
            - name: config
              mountPath: /config/config.yaml
              subPath: frigate.yml
              readOnly: true
          env:
            - name: TZ
              value: America/Denver
            - name: USE_FP16
              value: 'False'
            - name: NVIDIA_VISIBLE_DEVICES
              value: all
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: compute,video,utility
            - name: FRIGATE_REOLINK_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: frigate-secret
                  key: FRIGATE_REOLINK_PASSWORD
          ports:
            - name: https
              containerPort: 8971
            - name: rtsp
              containerPort: 8554
            - name: webrtc
              containerPort: 8555
            - name: webrtc-udp
              protocol: UDP
              containerPort: 8555

---
apiVersion: k8s.bitwarden.com/v1
kind: BitwardenSecret
metadata:
  name: secret
  annotations:
    argocd.argoproj.io/sync-options: Replace=true
spec:
  organizationId: 575f69b2-49f4-456d-bd6f-b14101103188
  secretName: frigate-secret
  map:
    - secretKeyName: FRIGATE_REOLINK_PASSWORD
      bwSecretId: 4fb11013-6ee4-47d1-83b7-b1c6012061cc
  authToken:
    secretName: bw-auth-token
    secretKey: token

---
apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  selector:
    app.kubernetes.io/component: web
  ports:
    - name: https
      port: 443
      targetPort: https

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: 'HTTPS'
    nginx.ingress.kubernetes.io/proxy-ssl-verify: 'off'
    cert-manager.io/cluster-issuer: cloudflare-issuer
spec:
  ingressClassName: internal
  rules:
    - host: &host frigate.beaver-cloud.xyz
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: web-service
                port:
                  name: https
  tls:
    - hosts: [*host]
      secretName: *host
