---
mqtt:
  enabled: false

detect:
  enabled: false

auth:
  enabled: false

cameras:
  doorbell:
    ffmpeg:
      inputs:
        - path: rtsp://admin:{FRIGATE_REOLINK_PASSWORD}@192.168.50.1/Preview_01_main
          roles:
            - record
        # - path: rtsp://127.0.0.1:8554/doorbell_ext
        #   input_args: preset-rtsp-restream
        #   roles:
        #     - detect
  garage:
    ffmpeg:
      inputs:
        - path: rtsp://admin:{FRIGATE_REOLINK_PASSWORD}@192.168.50.2/Preview_01_main
          roles:
            - record
        # - path: rtsp://127.0.0.1:8554/garage_ext
        #   input_args: preset-rtsp-restream
        #   roles:
        #     - detect
