---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  - patch: |-
      - op: add
        path: /spec/template/spec/containers/0/envFrom/0
        value:
          configMapRef:
            name: app-env
    target:
      kind: Deployment
      name: app-deployment
