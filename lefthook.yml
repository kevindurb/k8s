---
pre-commit:
  parallel: true
  jobs:
    - run: npx prettier --write {staged_files}
      glob: '*.{yaml,yml}'
      stage_fixed: true
    - run: ansible-lint --fix {staged_files}
      glob: './ansible/**/*.{yaml,yml}'
      stage_fixed: true
