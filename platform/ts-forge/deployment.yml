---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
  labels: &labels
    app.kubernetes.io/component: app
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels: *labels
  template:
    metadata:
      labels: *labels
    spec:
      volumes:
        - name: home
          persistentVolumeClaim:
            claimName: home
      containers:
        - name: app
          image: ghcr.io/kevindurb/ts-forge
          env:
            - name: TZ
              value: America/Denver
            - name: TS_AUTHKEY
              valueFrom:
                secretKeyRef:
                  name: ts-auth-key
                  key: TS_AUTHKEY
          envFrom: []
          volumeMounts:
            - name: home
              mountPath: /home/git
          resources:
            requests:
              memory: 128M
            limits:
              memory: 256M

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home
  labels:
    app.kubernetes.io/component: app
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn-replicated
  resources:
    requests:
      storage: 20Gi

---
apiVersion: k8s.bitwarden.com/v1
kind: BitwardenSecret
metadata:
  name: ts-auth-key
  annotations:
    argocd.argoproj.io/sync-options: Replace=true
  labels:
    app.kubernetes.io/component: app
spec:
  organizationId: 575f69b2-49f4-456d-bd6f-b14101103188
  secretName: ts-auth-key
  map:
    - secretKeyName: TS_AUTHKEY
      bwSecretId: 218c3c5c-d865-4fe1-a328-b4a30124ad16
  authToken:
    secretName: bw-auth-token
    secretKey: token
