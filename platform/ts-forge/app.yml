---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ts-forge
  namespace: argocd
spec:
  project: apps
  sources:
    - repoURL: https://github.com/kevindurb/k8s.git
      targetRevision: HEAD
      path: platform/ts-forge
  destination:
    server: https://kubernetes.default.svc
    namespace: ts-forge
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      enabled: true
      prune: false
      selfHeal: true
