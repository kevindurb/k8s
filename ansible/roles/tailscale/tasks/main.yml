# SPDX-License-Identifier: MIT-0
---
- name: Enable tailscale
  ansible.builtin.service:
    name: tailscaled
    state: started
    enabled: true

- name: Up tailscale
  ansible.builtin.command: tailscale up --advertise-exit-node --advertise-routes 192.168.0.0/16 --ssh
  changed_when: false
