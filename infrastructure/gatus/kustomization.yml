---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gatus
namePrefix: gatus-

resources:
  - ../../bases/app-with-storage
  - ./rbac.yml

components:
  - ../../components/internal-http-route

images:
  - name: app-image
    newName: ghcr.io/twin/gatus
    newTag: v5.34.0@sha256:3fff895e77d35ee62e898860f4613755bc2344127d93e3f326429d40270e2115

configMapGenerator:
  - name: config
    files:
      - ./config/gatus.yml

replacements:
  - sourceValue: gatus.beaver-cloud.xyz
    targets:
      - select:
          kind: HTTPRoute
        fieldPaths:
          - spec.hostnames.0

patches:
  - patch: |-
      - op: add
        path: /spec/template/spec/volumes/-
        value:
          name: config-map
          configMap:
            name: config
      - op: add
        path: /spec/template/spec/containers/0/volumeMounts/-
        value:
          name: config-map
          mountPath: /config/config.yaml
          subPath: gatus.yml
      - op: add
        path: /spec/template/spec/containers/0/env
        value:
          - name: GATUS_LOG_LEVEL
            value: DEBUG
          - name: GATUS_CONFIG_PATH
            value: /config
      - op: add
        path: /spec/template/spec/containers/0/resources
        value:
          requests:
            memory: 100M
          limits:
            memory: 500M
      - op: add
        path: /spec/template/spec/containers/0/ports/0/containerPort
        value: 8080
      - op: add
        path: /spec/template/spec/serviceAccountName
        value: service-account
    target:
      kind: Deployment
      name: app-deployment
  - path: ./k8s-sidecar.patch.yml
    target:
      kind: Deployment
      name: app-deployment

labels:
  - includeSelectors: true
    includeTemplates: true
    pairs:
      app.kubernetes.io/name: gatus
